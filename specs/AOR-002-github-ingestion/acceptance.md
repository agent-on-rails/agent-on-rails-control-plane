# Acceptance — AOR-002 GitHub Ingestion

- [ ] Webhooks with invalid signatures are rejected.
- [ ] Spec/task state changes reflect on Issues via labels and body fields.
- [ ] PR open/update/check events are ingested as engine events.
- [ ] Label set matches the canonical `aor:*` vocabulary.
- [ ] Integration tests simulate webhook → engine state transition for at least one happy path and one auth failure.
